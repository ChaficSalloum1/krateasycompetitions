import { sharedStyles } from "./design-system.js";
export const participantRecoveryHtml = `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Recover my next action · Krateasy</title><style>
${sharedStyles}*{box-sizing:border-box;min-width:0}body{margin:0;background:var(--canvas);color:var(--ink);overflow-wrap:anywhere}.skip{position:absolute;left:-9999px;top:8px;background:var(--surface);color:var(--ink);padding:12px 16px;border:2px solid currentColor;border-radius:8px}.skip:focus{left:8px}main{width:min(560px,100%);margin:auto;padding:32px 18px 64px}h1{font-size:clamp(36px,10vw,58px);line-height:1;letter-spacing:-.04em}p{line-height:1.55;color:var(--muted)}form{background:var(--surface);padding:24px;border-radius:18px;box-shadow:0 16px 45px #17201d16}label{display:grid;gap:8px;font-weight:800}input{font:700 18px/1.2 ui-monospace,monospace;padding:14px;border:2px solid #737d78;border-radius:10px;text-transform:lowercase;width:100%}button{margin-top:16px;border:0;border-radius:10px;padding:14px 18px;background:var(--accent);color:white;font:800 16px/1 system-ui;cursor:pointer}:focus-visible{outline:3px solid var(--accent);outline-offset:3px}.status{min-height:28px}.error{color:var(--error);font-weight:750}@media(max-width:320px){main{padding-inline:12px}form{padding:17px}}@media(prefers-reduced-motion:reduce){*{scroll-behavior:auto!important}}@media(prefers-contrast:more){:root{--muted:#3f4945}form,input,button{border:2px solid currentColor}}@media(forced-colors:active){form,input,button,.skip{border:2px solid CanvasText}}@media print{body{background:white}.skip,button{display:none}main{width:auto;padding:0}form{box-shadow:none;border:2px solid currentColor}}
</style></head><body><a class="skip" href="#main">Skip to recovery form</a><main id="main"><p><strong>Krateasy Competitions</strong></p><h1>Recover my private next action</h1><p>Scan the event QR on any device, then enter the recovery code supplied privately by the organiser. The event code never lists participants.</p><form id="recover"><label>Private recovery code<input id="code" name="code" required autocomplete="one-time-code" spellcheck="false" pattern="kpr1_[a-f0-9]{32}" maxlength="37"></label><button type="submit">Show my next action</button><p id="status" class="status" role="status" aria-live="polite" aria-atomic="true"></p></form><p>If you do not have your private code, ask the event desk. Staff can issue a replacement without searching a public participant directory.</p></main><script>
const form=document.querySelector('#recover'),status=document.querySelector('#status');form.addEventListener('submit',async event=>{event.preventDefault();status.className='status';status.textContent='Checking your private code…';const query=new URLSearchParams(location.search),competition=query.get('competition'),revision=Number(query.get('revision')),code=document.querySelector('#code').value.trim().toLowerCase();if(!competition||!Number.isSafeInteger(revision)){status.className='status error';status.textContent='This event recovery link is incomplete. Ask the organiser for a new QR code.';return}try{const response=await fetch('/v1/competition-journey/'+encodeURIComponent(competition)+'/participant-recover',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({expectedOperationalRevision:revision,code})}),value=await response.json();if(!response.ok)throw Error();location.assign(value.path)}catch{status.className='status error';status.textContent='That code cannot be used. Check it carefully or ask the event desk for a replacement.'}});
</script></body></html>`;
