/**
 * J5 · Closed Evidence Record. The receipt is a projection of a completed,
 * server-owned closure; it neither calculates results nor certifies a plan.
 */
export function renderCloseIntegrityReceipt(competitionId: string): string {
  const encodedId = JSON.stringify(competitionId).replaceAll("<", "\\u003c");
  return `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Close &amp; Integrity Receipt · Krateasy</title><style>
:root{color-scheme:light;--ink:#17201d;--muted:#617068;--canvas:#f4f2ec;--paper:#fffefa;--line:#d8d7cf;--green:#155f49;--green-soft:#e7f4ed;--accent:#c95635;--danger:#a2352d;font-family:Inter,"Avenir Next",system-ui,sans-serif}*{box-sizing:border-box;min-width:0}body{margin:0;background:var(--canvas);color:var(--ink);overflow-wrap:anywhere}.skip{position:absolute;left:-9999px;top:8px;background:var(--paper);color:var(--ink);padding:12px;border:2px solid var(--accent);border-radius:10px}.skip:focus{left:8px}:focus-visible{outline:3px solid var(--accent);outline-offset:3px}.shell{max-width:970px;margin:auto;padding:28px 20px 64px}.top{display:flex;justify-content:space-between;gap:18px;align-items:center;border-bottom:1px solid var(--line);padding-bottom:16px}.brand{color:var(--ink);font-weight:850;text-decoration:none}.surface{font-size:12px;font-weight:800;color:var(--green);background:var(--green-soft);border-radius:999px;padding:7px 10px}.hero{padding:28px 0 20px}.eyebrow{font-size:11px;font-weight:850;letter-spacing:.12em;text-transform:uppercase;color:var(--accent)}h1{font-size:clamp(34px,5vw,56px);letter-spacing:-.055em;line-height:1;margin:9px 0}h2{font-size:19px;margin:0 0 9px}p{color:var(--muted);line-height:1.5}.grid{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:14px}.card{background:var(--paper);border:1px solid var(--line);border-radius:17px;padding:19px}.metric{font-size:31px;font-weight:850;letter-spacing:-.04em}.proof{font-size:12px;color:var(--muted);overflow-wrap:anywhere}.actions{display:flex;gap:10px;flex-wrap:wrap;margin-top:14px}.button{min-height:44px;display:inline-flex;align-items:center;justify-content:center;padding:0 14px;border-radius:10px;border:1px solid var(--line);background:white;color:var(--ink);font:inherit;font-weight:800;text-decoration:none;cursor:pointer}.button.primary{background:var(--accent);border-color:var(--accent);color:white}.checks{display:grid;gap:9px;margin:14px 0}.checks label{display:flex;gap:8px;font-size:13px}.error{background:#fff0ed;color:#8a2b1f;padding:13px;border-radius:10px}.success{background:var(--green-soft);color:var(--green);padding:13px;border-radius:10px}.field{display:grid;gap:6px;font-weight:750;margin-top:10px}.field input{min-height:44px;border:1px solid var(--line);border-radius:9px;padding:9px;font:inherit}@media(max-width:700px){.shell{padding:18px 14px 48px}.top{align-items:start;flex-direction:column}.grid{grid-template-columns:1fr}}@media(max-width:320px){.shell{padding-inline:10px}.card{padding:15px}}@media(prefers-reduced-motion:reduce){*{scroll-behavior:auto!important;transition:none!important}}@media(prefers-contrast:more){:root{--muted:#36443d;--line:#59635e}.card{border:2px solid var(--ink)}}@media(forced-colors:active){.card,.button,.skip{border:2px solid CanvasText}}@media print{body{background:white}.actions,form,.skip{display:none}.shell{padding:0}.card{break-inside:avoid}}
</style></head><body><a class="skip" href="#main">Skip to Close Receipt</a><main id="main" tabindex="-1" class="shell"><header class="top"><a class="brand" href="/">Krateasy Competitions</a><span class="surface">Close &amp; Integrity Receipt</span></header><section id="receipt" role="status" aria-live="polite"><p>Loading closure evidence…</p></section></main><script>
const competitionId=${encodedId};const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
async function post(path,body){const r=await fetch('/v1/competition-journey/'+encodeURIComponent(competitionId)+'/'+path,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(body)}),v=await r.json();if(!r.ok)throw Error(v.error||'command_rejected');return v}
function fault(error){return 'The exact closure command was not accepted. Nothing changed.'}
function render(view){const root=document.querySelector('#receipt'),c=view.closure;if(!c){const live=view.live;const ready=Boolean(view.publication&&live);const close=ready?'<section class="card"><div class="eyebrow">Close event</div><h2>Confirm the final integrity record</h2><p>Closure is available only after every contest is terminal, actual truth is complete, no dispute remains and delivery evidence is reconciled.</p><div class="checks" id="acks">'+['FINAL_RESULTS_REVIEWED','OPEN_DISPUTES_RESOLVED','MANUAL_RECORDS_RECONCILED','DELIVERY_EVIDENCE_REVIEWED'].map(code=>'<label><input type="checkbox" value="'+code+'"><span>'+code.replaceAll('_',' ')+'</span></label>').join('')+'</div><button class="button primary" id="close">Create immutable closure</button><p class="proof" id="close-status" role="status"></p></section>':'<section class="card"><div class="eyebrow">Not ready to close</div><h2>Live operation has not started</h2><p>There is no closure evidence until an authoritative published revision reaches terminal, reconciled actual truth.</p></section>';root.removeAttribute('role');root.innerHTML='<section class="hero"><div class="eyebrow">Closed Evidence Record</div><h1>'+esc(view.name)+'</h1><p>Closure has not yet been created for this competition.</p><div class="actions"><a class="button" href="/competitions/'+encodeURIComponent(competitionId)+'">Return to Organiser Studio</a></div></section>'+close;document.querySelector('#close')?.addEventListener('click',async e=>{const codes=[...document.querySelectorAll('#acks input:checked')].map(x=>x.value);e.currentTarget.disabled=true;document.querySelector('#close-status').textContent='Verifying terminal truth and independently rebuilding Guard…';try{await post('close',{expectedPublishedRevision:view.publication.revision,expectedOperationalRevision:live.publication?.revision??view.publication.revision,expectedLiveVersion:live.state.version,acknowledgedCodes:codes});location.reload()}catch(error){document.querySelector('#close-status').textContent=fault(error);e.currentTarget.disabled=false}});return}root.removeAttribute('role');root.innerHTML='<section class="hero"><div class="eyebrow">Closed Evidence Record</div><h1>'+esc(view.name)+'</h1><p>Published revision '+esc(c.publishedRevision)+' · operational revision '+esc(c.operationalRevision)+' · closed '+esc(c.closedAt)+'</p><div class="actions"><a class="button" href="/competitions/'+encodeURIComponent(competitionId)+'">Open Organiser Studio</a><a class="button primary" href="/v1/competition-journey/'+encodeURIComponent(competitionId)+'/closure-bundle?closure='+encodeURIComponent(c.closureHash)+'">Download evidence bundle</a></div></section><section class="grid"><article class="card"><div class="eyebrow">Settled results</div><div class="metric">'+esc(c.resultSummary.total)+'</div><p>'+esc(c.resultSummary.completed)+' completed · '+esc(c.resultSummary.walkovers)+' walkovers · '+esc(c.resultSummary.retirements)+' retirements</p></article><article class="card"><div class="eyebrow">Open items</div><div class="metric">'+esc(c.resultSummary.unresolved)+'</div><p>Unresolved result records</p></article><article class="card"><div class="eyebrow">Delivery evidence</div><div class="metric">'+esc(c.deliveryEvidence.length)+'</div><p>Bound message receipts</p></article></section><section class="card" style="margin-top:14px"><div class="eyebrow">Integrity proof</div><h2>Replayable, content-addressed closure</h2><p class="proof">Closure hash <code>'+esc(c.closureHash)+'</code></p><p class="proof">Results hash <code>'+esc(c.resultSummary.resultsHash)+'</code></p><details><summary>Authority proof set</summary><p class="proof">'+Object.entries(c.authority).map(([k,v])=>esc(k)+': '+esc(v)).join('<br>')+'</p></details></section><section class="card" style="margin-top:14px"><div class="eyebrow">Clean edition</div><h2>Duplicate the proven setup, not live truth</h2><p>Sources and approved decisions are carried forward. Results, operations, approval and publication are never copied.</p><form id="duplicate"><label class="field">New competition name<input id="name" required minlength="2"></label><label class="field">Event date<input id="date" required type="date"></label><div class="actions"><button class="button primary">Create clean draft</button></div><p class="proof" id="duplicate-status" role="status"></p></form></section>';document.querySelector('#duplicate').addEventListener('submit',async e=>{e.preventDefault();const button=e.currentTarget.querySelector('button'),name=document.querySelector('#name').value.trim(),eventDate=document.querySelector('#date').value,notice=document.querySelector('#duplicate-status');button.disabled=true;notice.textContent='Creating a clean, versioned draft…';try{const created=await post('duplicate',{expectedClosureHash:c.closureHash,name,eventDate});notice.innerHTML='Clean draft created. <a href="/competitions/'+encodeURIComponent(created.id)+'">Open '+esc(created.name)+'</a>'}catch(error){notice.textContent=fault(error);button.disabled=false}})}
function collapseStableEvidence(){const heading=[...document.querySelectorAll('#receipt h2')].find(node=>node.textContent==='Replayable, content-addressed closure'),card=heading?.closest('.card'),details=card?.querySelector('details');if(!details)return;details.querySelector('summary').textContent='Technical closure and authority evidence';card.querySelectorAll(':scope > p.proof').forEach(node=>details.append(node))}
fetch('/v1/competition-journey/'+encodeURIComponent(competitionId),{cache:'no-store'}).then(async r=>{const v=await r.json();if(!r.ok)throw Error(v.error||'not_found');return v}).then(view=>{render(view);collapseStableEvidence()}).catch(error=>document.querySelector('#receipt').innerHTML='<p class="error" role="alert">The closure receipt could not load. Nothing was changed.</p>');
</script></body></html>`;
}
