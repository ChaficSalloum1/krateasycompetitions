# Krateasy Competitions requirements traceability

Audit date: 15 September 2026
Baseline commit: `7e3a10d`
Authorities: A1–A9 in `PRD-Krateasy-Competitions-Execution-Control.md`

## Audit method and status rules

This is an executable-path audit, not a filename inventory. A requirement is **Implemented and connected** only when the authoritative create/operate journey exercises it. Passing unit or reference-fixture evidence without that path is **Implemented but disconnected** or **Reference/demo only**. No requirement is deprecated without a decision record; none currently has one.

Verification evidence:

- `./node_modules/.bin/tsc -b --pretty false` plus `TOURNAMENT_OS_CP_SAT_PYTHON=/Applications/Xcode.app/Contents/Developer/Library/Frameworks/Python3.framework/Versions/3.9/bin/python3.9 node --import tsx --test --test-concurrency=4 packages/*/test/*.test.ts apps/*/test/*.test.ts`: TypeScript build passed; 536 tests passed, 0 failed after the default web/Mac surfaces were isolated from reference truth. The pinned runtime is OR-Tools 9.15.6755; the host's `/usr/bin/python3` is currently unavailable until the Xcode licence is accepted.
- The installed Swift 6.4 compiler directly typechecked the complete Mac source module and the XCTest sources, including the new connected-default assertion. The last full SwiftPM gate was 44/44 before this slice; rerunning SwiftPM and `xcodebuild` is currently blocked by the host's unaccepted Xcode licence, which requires the user's own agreement.
- `xcodebuild -project apps/apple-client/TournamentOSClient.xcodeproj -scheme TournamentOSClient -destination 'platform=macOS' -derivedDataPath /private/tmp/krateasy-mac-import-derived CODE_SIGNING_ALLOWED=NO build`: `BUILD SUCCEEDED`.
- Playwright exercised the AI-free St Albans verification harness at 1440×900 and 390×844. The signed `/next` view showed the withdrawn pair at participant revision 2 and no further fixture; the venue projection showed published revision 1 plus operational revision 2; the organiser projection showed the identical operational revision, live proof, 48 participants and durable delivery counts. Browser console: 0 errors, 0 warnings.
- The durable Mac-journal gate also reopened the authoritative St Albans publication `encourt-padel-wellness-club-st-albans.36e218711a` at desktop and 390×844 mobile widths: the same page showed revision 1, `PUBLISHED`, Guard `PASSED`, and 108 required/108 scheduled contests with 0 browser errors and 0 warnings.
- The signed-pack gate reopened that identical publication in a fresh connected desktop browser and again showed revision 1, `PUBLISHED`, Guard `PASSED`, and 108/108 contests. The pack endpoint returned the same competition identity, publication revision 1 and current live head; the earlier 390×844 gate remains the mobile-web evidence because this slice did not change the publication page.
- The operational-safety browser gate exercised that same St Albans identity at desktop and 390×844. The venue display and signed participant `/next` both visibly showed `STOPPED · state 1` and the fixed instruction “Play is stopped. Follow venue staff instructions and await the next update.” while preserving published/operational revision 1 and the participant's exact next fixture. Browser console: 0 errors, 0 warnings.
- The signed printable-fallback browser gate exercised the exact St Albans identity at desktop and 390×844. It showed published revision 1/operational revision 1, the bound manual/signing/publication hashes, and the explicit blocked emergency-authority codes without inventing venue safety facts. The renderer produced 108 court assignments, 108 numbered score sheets, 48 opaque expiring participant QR cards and seven restoration steps. Browser console: 0 errors.
- The lifecycle browser gate reopened that exact St Albans identity at desktop and 390×844 after all 108 authoritative results were settled. The page visibly showed revision 1, `CLOSED`, Guard `PASSED`, 108 scheduled and 108 settled with zero unresolved, plus the bound machine/human evidence-bundle link. Browser console: 0 errors.
- The accessibility browser gate exercised the connected St Albans participant, organiser, venue, closure and printable fallback pages at 640px and 320px CSS widths (the WCAG 200%/400% reflow equivalents). Keyboard focus reached each skip link and moved to `main`; document `scrollWidth` equalled `clientWidth` at 320px while dense schedule tables remained locally scrollable. The accessibility tree exposed named status/proof/match regions; the fallback exposed 48 labelled QR images, 108 score sheets and table captions. The pass found and fixed a stale operational-revision URL in post-repair participant/QR links and an unreadable venue status colour. Browser console: 0 errors.
- The creation browser gate exposed Describe, Quick, JSON, YAML, CSV and XLSX as one accessible input group, previewed a YAML competition with all eight policy decisions visible, and saved authoritative draft revision 1 through `/v1/competition-journey`; browser console: 0 errors. The source-import integration test proves the same preview/save routes with the complete 108-fixture St Albans YAML.
- The pre-implementation source was preserved without dependencies, generated output, build caches, local Xcode state, or secrets in baseline commit `38d92ec`.

Connected-slice evidence: `authoritative-publication.test.ts` exercises the public API and direct platform boundary against forged artefacts, coordinated missing contests, stale revisions, corrupted hashes, cross-organisation artefact IDs, compiler/approver and approver/publisher identity collisions, incorrect acknowledgements, duplicate commands, and restart/replay. `competition-journey.test.ts` creates a fresh draft, compiles 98 contests, independently reruns Guard, atomically seals approval/publication/outbox evidence, and reopens the identical published revision through a fresh repository instance. `APIClientTests.swift` proves the Mac sends source facts and then only competition identity, expected revision and acknowledgements; it never sends a specification, graph, schedule, simulation or Guard report. A live built-macOS rehearsal created `publication-boundary-rehearsal.1570667365`, published exact revision `1`, and opened `/competitions/publication-boundary-rehearsal.1570667365`. The Mac read back 47 entrants/98 matches and Certified evidence; the browser showed the identical ID/revision as `PUBLISHED`, Guard `PASSED`, 98 required/scheduled contests, Guard hash `c5b8c48a1852d002f155fe623deca14716169aea1641320e23c2dc88083c1b9e`, definition hash `64e3eb6a1330059f632324fd04db13058816c50f3dfbbf107a6dec405269ac7d`, publication certificate hash `8654272d46ad46e0a24ec35baea305d91314a7aaabe4f7c3837be532aefef972`, and outbox key `publication-boundary-rehearsal.1570667365:v1`.

Workbench-slice evidence: `st-albans-workbench.test.ts` preserves the exact first pilot source and SHA-256 `f86b1d34939f8f4b0ba158b3613b95bc2d5d74d9b921413850b3b377f8d541a5`, independently derives 48 pairs, 13 pools, 66 group fixtures, 42 Konnect/Tower fixtures and 108 total fixtures, quarantines all 12 supplied audit claims, and exposes eight unresolved critical decisions rather than inventing policy. Structured edits produce a non-mutating semantic/operational preview, accept only registered values, become a second hashed source, and converge on the existing canonical `TournamentDefinition`. The server translates and independently validates the exact pool membership and proposed schedule against changing court availability, variable duration, six hard final-court rules and the 20:00 stop; Guard passes 108/108 only after every decision is explicit. Missing fixtures, forged final courts, conflicting sources, forged audit assertions, changed previews and unregistered policies fail closed. Identical input replays the same spec/graph/schedule/Guard hashes, and the published exact revision plus outbox reopens byte-identically after restart with no AI dependency.

Live-slice evidence: `live-no-show-journey.test.ts` activates the published St Albans revision through the existing live event kernel, records completed and in-progress facts, and previews two deterministic no-show/withdrawal choices without mutating live truth. Both choices independently rerun the full Competition Guard and a live replay/preservation/assignment Guard; both prove the zero-move lower bound, explain consequences and preserve completed/in-progress values byte-for-byte. Self-approval and stale proposals fail closed. Separate approval atomically persists operational revision 2 with the selected live state and outbox intents for every and only directly affected entrant. Duplicate commands replay idempotently, privileged or direct-withdrawal client input is rejected, and restart independently replays the event hash chain even when an attacker recomputes the outer storage hashes. All compile, Guard, repair and publication decisions remain deterministic with no AI dependency.

Live-information evidence: `participant-information-journey.test.ts` runs the full St Albans revision 1 publication, activation, completed and in-progress match facts, guarded no-show approval to operational revision 2, signed participant reads, minimal public/organiser projections and targeted delivery. Exactly the repair's affected participant and contest projection hashes change; stale revisions, forged tokens, cross-organisation access and unpublished proposal state fail closed. The mutable attention singleton and its HTTP routes are removed. Outbox messages are persisted in the same sealed journey write, fail/retry deterministically through the existing replay-safe delivery worker, and retain attempts, leases, provider idempotency keys and receipts across restart. Connected check-in, call, start, score, finish, walkover-via-approved-repair and score correction append immutable hash-chained facts; duplicate commands and post-repair restart replay to the same proof.

Participant credential evidence: `participant-access-lifecycle.test.ts` rotates one St Albans participant from a retained v1 verification key to the active v2 signing key, atomically revokes that participant's older access and recovery credentials, leaves another participant unaffected, and rejects the old/revoked links before and after restart. Content-bound command IDs make rotation, revocation and recovery-code issuance idempotent while conflicting reuse fails closed; independently checked access-event lineage rejects a re-hashed attempt to remove revocation state. A generic event QR carries only competition identity and current operational revision. The participant exchanges a private expiring code for the same minimal `/next` projection available to staff, never a public directory; wrong, forged, expired, cross-organisation and stale-head attempts fail closed under a five-attempt/minute opaque-client rate bucket. The accessible recovery page exposes a uniform failure state and produced zero browser console errors or warnings.

Closure evidence: `competition-close-journey.test.ts` takes the exact published St Albans competition through operational revision 2 and all 108 actual results. Close fails on incomplete contests, pending protest/appeal, stale heads, self-approval and incorrect acknowledgements, then atomically seals the final result/operation/live proofs and closure outbox intent. Export contains preserved sources plus the canonical specification, graph, schedule, simulation, independently rerun Guard, approval/publication history, actual/live/operational truth, closure, authoritative record and human audit. A forged inner artifact fails even when its outer hash is recomputed. A fresh isolated repository restores, independently replays and re-exports the byte-identical bundle. Duplicate creates a clean 2027 draft with explicit closure/source/decision provenance and name/date edits, no carried publication/live/results, then recompiles to the same 108-contest structure. Retry, restart, cross-organisation and post-close mutation attacks fail closed. Actual winner propagation now traverses canonical bye nodes, so the connected final result set proves the full Konnect/Tower path rather than simulated identities.

Accessibility evidence: `pilot-accessibility.test.ts` makes the connected live-information accessibility envelope executable: language/viewport/landmark/skip-link structure, named dynamic regions, semantic time/table/QR output, zoom permission, 320px reflow CSS, focus visibility, reduced motion, increased contrast, forced colours, print rules and computed WCAG AA normal-text colour pairs. `offline-event-pack-journey.test.ts` applies the same checks to the generated 108-fixture printable pack. `participant-information-journey.test.ts` proves refreshed direct and QR access links bind operational revision 2 after the no-show repair while their signed grant remains publication scoped. Browser keyboard/reflow/accessibility-tree evidence covers the exact closed St Albans identity. Named VoiceOver/NVDA, printed-paper and representative outdoor-device inspection remain an owner-run acceptance gate and are not claimed by automated evidence.

Multi-source import evidence: `creation-source-import.test.ts` feeds exact YAML, CSV and actual XLSX ZIP bytes through the authoritative journey. Exact source text or XLSX bytes and SHA-256 survive restart; 192 entrant-field facts retain and merge provenance. CSV/XLSX reuse `importEntrantsCsv`; mismatched fields block compilation. Formula values/cells, active workbook content, YAML aliases/anchors/tags, duplicate entrant/member identities, unsafe or over-expanded archives, invalid/oversized payloads and extra authority fields quarantine or reject without contributing facts. The latest source can be removed with optimistic concurrency, restoring the earlier converged workbench without rewriting it. The patched YAML dependency audit reported zero known vulnerabilities.

Mac source and duplication evidence: `APIClientTests.swift` proves the connected Mac transports exact JSON, YAML and CSV text or XLSX bytes through the authoritative multi-source boundary without native interpretation. Unsupported types, oversized files and non-UTF-8 text fail before transport. A second source appends with optimistic revision identity; a clean-edition command first reads the authoritative closed head and then sends only its closure hash plus the new name/date. The default app now constructs only the connected local-server session; local drafts and seeded profiles remain reference-test code and cannot be selected by the pilot runtime.

Runtime-isolation evidence: `authoritative-surface.test.ts` proves `/` lists only durable `CompetitionJourney` identities while `/demo` is explicitly separate. In production, the root fails closed until a real pilot web projection is configured, and `/demo`, `/lab`, `/api/demo` and `/api/workspace` cannot return reference truth. `AppShellTests.swift` asserts the default Mac workspace list contains exactly the connected local compiler and no demo client.

Web repeat-event evidence: the closed authoritative competition page conditionally exposes an accessible clean-edition form, describes the truth that will and will not be carried forward, and submits exactly the server-read closure hash plus new name/date to the existing duplicate boundary. The API journey tests prove stale closure, extra authority, replay and inherited-live-state attacks fail closed; the markup contract verifies labelled fields, live status and the minimal request shape.

Independent path evidence: Guard 1.2 executes `guard-path-reconstruction.ts` without calling the graph/topology compiler, scheduler, simulation or their proof hashes. For the pilot primitives it reconstructs pool pair coverage and allocation shape, elimination opening entrants/byes, the exact previous-round winner partition at every cup round, edge/slot closure and the full source-stage-to-opening-round qualification dependency set. `competition-guard.test.ts` proves coordinated feeder rewires, duplicate opening entrants and forged qualification edges remain blocked even when contest counts, graph hashes and client-side edges are made mutually consistent. The 108-fixture St Albans compile, close and restore journeys remain green under the new rule.

Pilot release evidence: `pilot-release-readiness.ts` consumes only the verified server-owned closure evidence bundle, derives an exact organisation/competition/published/operational/live scope hash, rechecks the 108-result accounting and Guard 1.2 binding, and then evaluates the external gates without allowing an acceptance to provide core artifacts or fallback text. `competition-close-journey.test.ts` proves the complete software rehearsal remains `BLOCKED_EXTERNAL_AUTHORITY` with every missing owner and fixed safe fallback, wrong-scope evidence fails closed, acceptance order is deterministic, and only a complete fresh exact-role/exact-scope evidence set can report `READY`. `docs/pilot-release-readiness.md` records the current limitations and rollback sequence without inventing human approval, credentials or venue facts.

Publication-diff evidence: `createPublicationChangeSet` deterministically derives exact definition changes and added, removed, reassigned, retimed and duration-changed contest identities. The connected workbench retains the organiser-reviewed semantic and operational preview, and the server binds its change-set hash into the independent approval, certificate version 1.1, publication outbox and closure bundle. Platform publication independently derives the first-publication baseline from the server-owned tournament revision and artefacts. Certificate and release verification reject a changed or mismatched change set even when bundle checksums are recomputed.

Guard pre-flight evidence: Guard 1.2 derives a bottom-up ledger for every division/stage/pool/round, including exact required and scheduled contest identities, contest minutes, changeover minutes and scheduled occupancy, and reconciles it with every resource unit's available, occupied and spare capacity. Missing, duplicate, unexpected or duration-divergent contests make accounting non-reconciled. `createCompetitionGuardPreflight` projects the same immutable report into a short decision, expandable sections/findings/accounting and exact technical bindings. The journey DTO and `/competitions/:id/preflight` organiser page expose that projection, stable evidence hashes and suggested corrections; integrity and operational quality remain visibly separate.

Actual-result progression evidence: `live-progression-journey.test.ts` records all 66 St Albans group results through the connected command boundary, independently recalculates standings and the approved percentage-normalised Konnect/Tower qualification, and appends server-owned entrant-resolution facts over the unchanged published topology. A knockout cannot start while identities are unresolved, clients cannot submit resolution facts, forged simulation-selected entrants cannot score, actual winners propagate to the next round, participant `/next` shows the resolved contest, and fresh-process replay reproduces the identical state proof. Published possible-entrant sets remain an over-approximation for scheduling; they are never treated as actual live identity.

Court-outage evidence: `live-court-outage-journey.test.ts` starts from the exact St Albans publication, approves the existing no-show release as operational revision 2, then closes an affected court and derives the repair problem entirely on the server from the current assignments, authoritative calendars, live state and hard finish. The deterministic lexicographic repair reuses released capacity, recomposes all 108 Guard-visible contests, preserves completed/in-progress truth, independently reruns schedule validation and Competition Guard, explains the affected scope, requires a different approver, and atomically publishes operational revision 3 plus targeted participant outbox intents. Preview emits no notification; already-called promises, stale revisions and proposer-owned repair/Guard fields fail closed; duplicate proposal and approval commands are content-bound/idempotent; and restart regenerates the proposal so a forged repair remains invalid even when every outer storage hash is recomputed. A capacity-tight outage with no proven repair remains blocked rather than weakening the 20:00 hard stop or final-court requirements.

Delay/overrun evidence: `live-delay-overrun-journey.test.ts` records a real St Albans contest as in progress, releases capacity through the approved no-show revision, then reports a typed expected overrun. The server derives the affected court window from the authoritative contest’s planned end and actual court, leaves the in-progress fact byte-identical, deterministically moves the downstream contest, recomposes 108 Guard-visible assignments, reruns schedule and live Guards, requires independent approval and atomically publishes only affected projections as operational revision 3. Non-live contests, stale heads and client-supplied repair/Guard artefacts fail closed; duplicate approval and fresh-process replay are deterministic.

Mac offline-command evidence: `OfflineCommandQueueTests.swift`, `APIClientTests.swift` and `AppShellTests.swift` exercise the actual Mac queue, HTTP transport and application model. The app persists a per-workspace/per-competition 0600 journal with a canonical SHA-256 integrity seal, recovers interrupted sends to pending without losing the idempotency key, never re-emits an acknowledged command after restart, preserves command order behind conflicts, bounds payloads, and rejects a tampered or cross-scope journal. Check-in, call, start, score, finish, walkover and correction encode the strict connected command schema without client authority fields. Transport failure stays pending; a stale server head becomes an explicit conflict; reconciliation rotates the idempotency key and expected live version inside the exact payload rather than overwriting server state. The connected control room now projects all authoritative live contests and their true live version, and visibly exposes durable status, sync and reconciliation.

Signed offline-event-pack evidence: `offline-event-pack-journey.test.ts`, `APIClientTests.swift` and `AppShellTests.swift` exercise the server issuer, HTTP boundary, Mac verifier, scoped 0600 cache and restart path. The server accepts only expected publication revision, expected operational revision and expiry, then derives 108 public contests and 48 participant lookup rows from the exact authoritative publication and current operational/live head. It signs the exact payload bytes with Ed25519; the Mac requires a separately pinned public key and rejects missing trust, tampering, wrong scope, stale revisions, expiry and unpublished state. Identical state issues byte-identical packs across restart, and an offline Mac restart loads the verified cache without a network fetch. A replay regression also proves the activated zero-event live proof is canonical after restart. Missing St Albans emergency contacts, venue address, responder roles, access and evacuation instructions remain explicit blocking authority codes; the software does not invent them or claim emergency readiness.

Printable manual-fallback evidence: `manual-fallback-pack.ts`, `manual-fallback-view.ts`, `offline-event-pack-journey.test.ts`, `OfflineEventPack.swift` and `APIClientTests.swift` derive and verify a deterministic pack inside the signed exact-revision offline envelope. The server, never the client, constructs the complete order of play, court sheets, one manual score sheet per fixture, privacy-preserving expiring participant QR index and ordered restoration checklist; the Mac verifies the bound coverage and opens only the identity/revision URL. Removing a score sheet, changing a hash, adding an injected query field, requesting a stale revision or using a forged signature fails closed. The print view prominently separates ready operational materials from blocked emergency-authority data, so it cannot be mistaken for the venue's separately controlled safety plan.

Operational-safety evidence: `operational-safety.test.ts` and `operational-safety-journey.test.ts` exercise a separate, append-only operational authority state inside the same competition record. Normal, Degraded, Paused, Stopped, Cancelled and Recovering transitions bind an actor, exercised function, reason, effective time, scope, expected state version, idempotency identity and approved public-message code. Typed internal incidents do not leak into public or participant delivery. Safety or incident authority can stop immediately; stopped/cancelled state blocks new contest starts and live-revision publication. Restart requires evidence-bearing safety and competition clearances before the incident lead can enter Recovering and then Normal. Authority transfer is explicit and immediately invalidates the prior actor. Every public transition atomically emits exactly 48 minimal St Albans status/projection intents, survives restart with the identical proof and rejects stale, reused, re-hashed or client-attributed commands. The signed Mac pack, `/next`, venue and organiser surfaces carry the same state/proof; the offline pack rejects internally inconsistent signed projections.

Offline incident evidence: `OfflineCommandQueueTests.swift`, `APIClientTests.swift`, `AppShellTests.swift` and the connected HTTP safety test exercise a typed incident plus optional venue stop through the same scoped 0600 Mac journal. Both drafts persist in one local batch and reopen after process restart. Strict transport routes each allowlisted command to its exact operational endpoint, rejects an altered envelope, reads the operational state head on conflict and updates root command identity/version only through explicit reconciliation. The Mac never supplies actor, authority, occurrence time, public-message code or next-update time; the server derives the assigned actor, acceptance time, fixed message and a fresh ten-minute update deadline when a delayed offline stop reconnects. The UI explicitly tells staff to give the local stop instruction immediately because queued truth is not public until server acknowledgement.

## Runtime truth and duplicated-state inventory

| Runtime path | Owning files | Audit status | Evidence and consequence |
|---|---|---|---|
| Canonical schema → graph → schedule → simulation → certification | `packages/tournament-schema/src/*`; `packages/competition-engine/src/scenario.ts`; `apps/compiler-web/src/{competition-journey,production-lock-definition}.ts` | Implemented and connected | The declared P&K benchmark and the 48-pair St Albans pilot both converge on the existing `TournamentDefinition`, compiler, graph, schedule validation, simulation and Guard. Other formats remain outside this slice. |
| Production-style `/v1/tournaments` reads | `apps/compiler-web/src/server.ts` | Implemented and connected | The portfolio and per-competition reads project persisted journey records only. `/` uses the same durable list, empty portfolios remain empty, unknown IDs return 404, and `runReferenceDemo()` is reachable only through explicitly named non-production reference surfaces. Production identity/persistence composition remains a deployment gate. |
| Web creation proposal/workbench | `apps/compiler-web/src/{creation-proposal,creation-source-ingestion,competition-workbench,competition-journey,creator-view}.ts` | Implemented and connected for pilot envelope | Describe/Quick and exact JSON/YAML/CSV/XLSX inputs enter one journey. Original bytes/text, hashes, quarantine findings and field provenance persist; spreadsheet evidence is values-only and formula/active-content safe, disagreements block compilation, and the most recent source commit is reversible. The closed page exposes closure-bound clean duplication into the same draft model. |
| Competition close, evidence and repeat | `apps/compiler-web/src/{competition-lifecycle,competition-journey,server}.ts`; `competition-close-journey.test.ts` | Implemented and connected | Exact terminal actual truth, resolved disputes, independent Guard, authority separation, freshness and acknowledgements seal one atomic closure. Hashed machine/human export restores into an isolated fresh store and replays byte-identically; clean duplication carries only explicit source/decision memory and provenance. |
| Web guided creator | `apps/compiler-web/src/creator-view.ts`; `competition-journey.ts` | Implemented and connected for draft entry | `/create` previews and saves through the authoritative journey; it no longer writes the in-memory platform-demo aggregate. The older portfolio wizard in `ui.ts` remains reference/demo only. |
| Registered language grammar | `packages/competition-engine/src/intent-compiler.ts:67`; `apps/compiler-web/src/server.ts:218-250` | Implemented but disconnected | Proposal-only and separate from `creation-proposal.ts` and legacy `compiler.ts`. |
| Legacy English interpreter | `packages/competition-engine/src/compiler.ts:31-90` | Implemented but disconnected | Third overlapping interpreter; used by engine tests, not the customer journey. |
| Seeded platform pilot | `apps/compiler-web/src/platform-demo.ts:78-254` | Reference/demo only | Preloads Play & Konnect, separately authors two live contests, then rehearses publish/repair. |
| Connected no-show/withdrawal journey | `apps/compiler-web/src/{competition-journey,no-show-journey,server}.ts` | Implemented and connected | The published canonical revision creates the live definition; strict commands preserve actual truth; deterministic repair options rerun Competition Guard plus live Guard; separate approval atomically publishes operational revision/outbox for directly affected consumers. |
| Connected court-outage and delay journey | `apps/compiler-web/src/{competition-journey,court-outage-journey,server}.ts`; `live-{court-outage,delay-overrun}-journey.test.ts` | Implemented and connected | The server derives affected-scope repairs from authoritative assignments, actual court state and changing calendars, proves the lexicographic repair, recomposes and Guards the complete schedule, preserves prior operational publications and in-progress truth, and atomically targets only affected participants after separate approval. Unproven or infeasible repairs remain blocked. |
| Participant `/next`, public/organiser projections and delivery | `apps/compiler-web/src/{participant-information,competition-journey,player-view,attention-views,server}.ts`; `participant-information-journey.test.ts` | Implemented and connected | Opaque signed expiring access returns one minimal participant projection from the exact publication plus operational head. Public and organiser views use the same derived contest/participant rows. Atomic targeted outbox state, retry and provider evidence survive restart; an external pilot provider and fallback policy still require an operational choice. |
| Actual-result standings, qualification and knockout progression | `apps/compiler-web/src/{live-progression,competition-journey}.ts`; `live-progression-journey.test.ts` | Implemented and connected | The live head derives actual Konnect/Tower identities from completed group scores through the existing standings, qualification and graph core. Resolution is server-owned, possible entrants cannot impersonate actual qualifiers, winner paths advance deterministically, and replay independently verifies the derived mapping. |
| Mac creation | `apps/apple-client/.../{AppShell,FeatureViews}.swift` | Partial | The connected workspace supports Describe/Quick, exact JSON/YAML/CSV/XLSX file import and appended sources, server interpretation review, editable required facts, visible assumptions, compile, Guard review, exact server publication and closure-bound clean duplication. Demo workspaces intentionally retain honest revision-zero local drafts. Richer general-format editing remains. |
| Mac network client | `apps/apple-client/.../TournamentAPIClient.swift`; `APIClientTests.swift` | Implemented and connected | Staged write/read DTOs preserve one server identity and revision; contract tests prove no proposer-owned Guard artefacts cross publication, import or duplicate boundaries. |
| Mac offline journal | `apps/apple-client/.../{OfflineCommandQueue,TournamentAPIClient,AppShell,FeatureViews}.swift` | Implemented and connected | The connected control room owns a scoped 0600 file journal for exact live, incident and stop commands, retry/conflict/reconciliation state and restart-safe acknowledgement. Incident+stop saves atomically; server authority, clocks and public messaging are never asserted locally. |
| Mac signed offline event pack and printable operational fallback | `apps/compiler-web/src/{offline-event-pack,manual-fallback-pack,manual-fallback-view,competition-journey,server}.ts`; `apps/apple-client/.../{OfflineEventPack,TournamentAPIClient,AppShell,FeatureViews}.swift` | Implemented and connected | Server-owned exact-revision projections and deterministic schedule/court/score/QR/restoration materials are Ed25519 signed, Mac-verified against pinned trust and atomically cached with 0600 permissions. Offline lookup and the printable route share the published/operational identity and live proof; missing safety authority data is explicit and blocking. |
| Operational safety state and incident history | `apps/compiler-web/src/{operational-safety,competition-journey,participant-information,server}.ts`; `operational-safety*.test.ts` | Implemented and connected | Six explicit states, typed internal incidents, five named authority functions, transfer, fixed public messages and two-clearance restart are hash-chained beside live truth. Public transitions atomically update all 48 participant/venue projections without exposing incident notes; Mac offline incident/stop writes reconnect through the strict server-owned boundary. Production identity composition remains open. |
| Organisation platform/event store | `packages/competition-engine/src/platform.ts`; `event-store.ts`; `postgres-event-store.ts` | Partial | `PUBLISH_TOURNAMENT` accepts identity/revision/acknowledgements only, loads tenant-bound authoritative artefacts, independently reruns Guard, enforces compiler/approver/publisher separation and approval freshness, and atomically appends publication plus its outbox intent. Broader platform compilation ownership remains incomplete. |
| Dynamic competitions | `dynamic-stage-*`; `dynamic-format.ts` | Implemented but disconnected | Swiss/ladder/heat/Americano engines are tested outside the normal organisation competition lifecycle. |

Five historical creation representations remain in source: `CompetitionBlueprint`, registered intent AST, legacy `IntentAst`, guided platform `JsonValue`, and Swift `LocalTournamentDraft`. The pilot runtime has one authority: the journey persists `CompetitionBlueprint`, JSON/YAML and entrant adapters converge through its workbench, and Swift transports that boundary. Guided `JsonValue`, seeded profiles and local drafts remain reference/test-only and are unreachable from the default web/Mac pilot surfaces; removing those preserved non-pilot capabilities would require an explicit product decision.

## Evidence key

| Key | Owning module and executable evidence |
|---|---|
| SCHEMA | `packages/tournament-schema/src/{types,compile,validate,type-checker,revisions,semantic-diff}.ts`; `packages/tournament-schema/test/schema.test.ts` |
| NL | `packages/competition-engine/src/{intent-compiler,compiler,compiler-critic}.ts`; corresponding tests; `apps/compiler-web/test/creation-proposal.test.ts` |
| GRAPH | `packages/competition-engine/src/{graph,pool-construction,pool-allocation,qualification,topology-compiler,draw-constraints}.ts`; engine/topology/pool/draw tests |
| SCHEDULE | `packages/competition-engine/src/{schedule-model,scheduler,schedule-solver,cp-sat-solver,lower-bounds,scheduling-quality}.ts`; scheduler/CP-SAT/adversarial tests |
| GUARD | `packages/competition-engine/src/{competition-guard,certification,platform}.ts`; `competition-guard.test.ts`; `authoritative-publication.test.ts` |
| SIM | `packages/competition-engine/src/{simulation,monte-carlo,operational-chaos}.ts`; simulation/chaos tests |
| LIVE | `packages/competition-engine/src/{live-operations,live-change,schedule-repair,tournament-state,adjudication}.ts`; matching tests |
| PLATFORM | `packages/competition-engine/src/{event-store,platform,platform-api,postgres-event-store}.ts`; platform/API/event/concurrency/authoritative-publication tests |
| CAPS | `packages/competition-engine/src/{capability-ledger,capability-conformance,format-capabilities,sport-semantics,sport-rule-packs,static-formats,dynamic-format}.ts`; conformance tests |
| ASSURANCE | `packages/competition-engine/src/{bounded-verification,generative-campaign,lifecycle-fuzz,whole-spec-verification,restore-drill}.ts`; matching tests |
| WEB | `apps/compiler-web/src/*`; `apps/compiler-web/test/*` |
| MAC | `apps/apple-client/Sources/TournamentOSClientCore/*`; `apps/apple-client/Tests/TournamentOSClientCoreTests/*` |

## A1 master specification: M00–M60

| ID | Source section | Plain-language requirement | Job / owner / surface | Status | Evidence, gap, phase |
|---|---|---|---|---|---|
| M00 | A1 §0 | English must traverse intent, ambiguity, canonical spec, validation, graph, schedule, simulation and certification. | J2–J6; journey/core; Mac+web | Partial | Connected for the explicit P&K milestone in `competition-journey.ts`; the general grammar and other formats are not yet connected. Stage 1–2. |
| M01 | A1 §1 | Produce the complete organiser blueprint and support non-destructive English change. | J3–J6; blueprint; Studio | Partial | `blueprint-view.ts`, semantic revisions; no complete fresh-input blueprint or connected modify flow. Stage 1–2. |
| M02 | A1 §2 | One engine for scenario, live, API and clients. | All; core; all | Partial | Mac/web creation, participant/public projections and live commands now use the existing schema/scenario/Guard/live core; legacy guided and seeded platform worlds remain separate. Stage 1–2. |
| M03 | A1 §3 | Keep qualification, seeding, topology, placement and scheduling separate; mark HARD/SOFT. | J4–J5; core; evidence | Implemented but disconnected | SCHEMA/GRAPH/SCHEDULE tests. Stage 1. |
| M04 | A1 §4 | Immutable, versioned canonical IR with hashes and provenance. | J3/J6; schema; all | Implemented and connected | The journey preserves each exact source/hash and field provenance, versions structured decisions, and persists immutable spec/graph/schedule/simulation/Guard/approval/publication hashes plus an exact-revision outbox intent; production storage adapter remains Stage 2. |
| M05 | A1 §5 | Sport is data; head-to-head and ranked performance are supported. | J12; sport packs; rules | Implemented but disconnected | CAPS conformance across named sports; creator does not resolve submitted sport to packs. Stage 4 gate: domain review. |
| M06 | A1 §6 | Composable participation, stage, qualification, bracket and resource primitives. | J4/J5/J12; core | Implemented but disconnected | CAPS and 16 primary primitive conformance fixtures. Stage 4 evidence gate. |
| M07 | A1 §7 | Static formats compose as DAGs without one assumed shape. | J4/J12; graph | Implemented but disconnected | GRAPH custom/static tests. Stage 1. |
| M08 | A1 §8 | Registered dynamic interface including result-dependent Americano. | J12; dynamic core | Implemented but disconnected | `dynamic-stage-*`, Americano tests; no platform binding. Deferred: after connected static slice and dynamic historical fixture gate. |
| M09 | A1 §9 | Staged extraction/interpretation and explicit ambiguity classes. | J2/J3; interpreter; creation | Partial | NL narrow grammar; web proposal re-parses separately and does not create spec. Stage 1. |
| M10 | A1 §10 | Every inference has inspectable origin and uncertainty class. | J3; ledger; review | Partial | Schema/intent evidence exists; web/Mac creation omit formal rule mapping. Stage 1. |
| M11 | A1 §11 | Compile safely as far as possible, ask only material decisions, recompile. | J3; journey; creation | Partial | Exact questions, versioned draft revision and re-review are connected on Mac; field-level decision commands and general partial compilation remain. Stage 1–2. |
| M12 | A1 §12 | Validated declarative DSL; never arbitrary executable policy. | J3/J12; schema/packs | Partial | JSON and YAML now converge on the same data-only St Albans workbench; YAML tags, anchors and aliases are quarantined. General advanced-editor round-trip remains Stage 4. |
| M13 | A1 §13 | Explicit rule priority and conflict evidence. | J4; draw | Implemented but disconnected | GRAPH draw-constraint tests. Stage 1. |
| M14 | A1 §14 | Pool optimiser returns chosen/alternative structures and proof. | J4; pool core | Implemented but disconnected | Pool construction/allocation tests; no fresh journey call. Stage 1. |
| M15 | A1 §15 | Composable deterministic standings metrics. | J4/J12; standings | Partial | The St Albans live head now uses the existing deterministic standings core to resolve real group results; other connected format registries remain Stage 4. |
| M16 | A1 §16 | Unequal pools require explicit normalisation; never raw totals. | J4; standings/Guard | Implemented and connected | St Albans compilation blocks without an explicit policy, then actual live qualification reuses the approved percentage normalisation across its unequal pools. Unsupported schema strategies still fail closed. |
| M17 | A1 §17 | Identity-free topology with arbitrary counts, byes/play-ins/conditionals. | J4; topology | Implemented but disconnected | Exhaustive 2–64 topology/nasty tests. Stage 1. |
| M18 | A1 §18 | Draw returns placement, alternatives, violations and replay proof. | J4; draw | Partial | Strong constraints/proofs; manual placement and consistent journey use incomplete. Stage 1–2. |
| M19 | A1 §19 | All possible participant dependencies constrain earliest starts. | J4/J5; graph/scheduler | Partial | Published possible-entrant sets protect scheduling; the live boundary now refuses a knockout start until actual qualification/winner dependencies resolve. Full independent Guard path reconstruction remains. |
| M20 | A1 §20 | Scheduling is explicit hard/soft optimisation behind an abstraction. | J5; scheduler | Partial | Typed exact/CP-SAT exist; default `runScenario` uses the simpler scheduler. Stage 1, then envelope validation. |
| M21 | A1 §21 | Explain resource, dependency, participant and restricted-resource lower bounds. | J5/J6; evidence | Implemented but disconnected | `lower-bounds.ts` tests; fresh journey does not surface it. Stage 2. |
| M22 | A1 §22 | Model rest/thermal/wait quality, not collision only. | J5; quality | Partial | Wait/back-to-back metrics exist; warm-up/court-change/max-consecutive and unified score incomplete. Stage 2. |
| M23 | A1 §23 | Classify/explain every resource interval and avoid mysterious idle. | J5; quality | Partial | Several states audited; held/rest-blocked distinctions and Guard integration missing. Stage 2. |
| M24 | A1 §24 | Structural, deterministic, random, adversarial and Monte Carlo dry runs use production logic. | J6/J9; simulation | Implemented but disconnected | SIM/ASSURANCE fixtures. Stage 2. |
| M25 | A1 §25 | Dry run executes/replays the real event state machine. | J6/J11; state | Partial | Graph simulator, live state and platform event model are separate. Stage 2–3. |
| M26 | A1 §26 | Universal invariants plus additive format/sport invariants. | All; Guard/core | Partial | Distributed strong checks; no single registry invoked by every write. Stage 1–3. |
| M27 | A1 §27 | Large generated property space proves conservation/termination/replay. | J12; assurance | Implemented but disconnected | ASSURANCE/campaign/property tests. Permanent release gate. |
| M28 | A1 §28 | Differential/metamorphic properties for resources/rest/IDs/seeds. | J12; assurance | Implemented but disconnected | Bounded and whole-spec metamorphic tests. Permanent release gate. |
| M29 | A1 §29 | Permanent P&K, Americano and generic historical regression corpus. | J12; fixtures | Partial and connected | P&K and the historical/dynamic corpus remain permanent regressions; `flexible-competition-journey.test.ts` now drives named six-pair round-robin and eight-pair single-elimination fixtures through the connected product lifecycle. Americano remains disconnected. Permanent gate. |
| M30 | A1 §30 | Staged certification state and structured report. | J6; Guard; review | Partial | Guard and solver states exist, not the full staged certification lifecycle. Stage 2. |
| M31 | A1 §31 | Deterministic `why()` for every consequential output. | J4–J6/J9; evidence | Partial | Proofs/debug IDs are widespread; comprehensive connected query is absent. Stage 2. |
| M32 | A1 §32 | Counterfactuals clone revisions and show diffs. | J6; scenarios | Implemented but disconnected | `scenario-comparison.ts`; UI uses fixed demo options. Stage 2. |
| M33 | A1 §33 | Human blueprint and machine spec/graph/schedule/certificate export. | J6/J11; export | Partial | `blueprint-view.ts`, `export-bundle.ts`; not from a fresh journey. Stage 2–3. |
| M34 | A1 §34 | Interactive understood/decision/choice/feasibility UI with progressive evidence. | J2–J6; Studio | Reference/demo only | `/lab` always shows reference P&K. Stage 1–2. |
| M35 | A1 §35 | English, visual graph and advanced editor round-trip one IR. | J3/J4; Studio | Partial | Graph display exists; editing/round-trip does not. Deferred entry gate: connected canonical draft plus observed expert need. |
| M36 | A1 §36 | Stable machine-readable compiler errors/warnings. | J3/J5; core | Implemented but disconnected | TSC/TSV/TSW evidence; creation has another question model. Stage 1. |
| M37 | A1 §37 | Infeasible requests receive nearest viable alternatives requiring approval. | J5/J9; repair | Partial | Connected no-show choices plus court-outage and delay repairs expose consequences and require approval; infeasible/unproven repairs fail closed. General specification and multi-option schedule alternatives remain. Stage 2–3. |
| M38 | A1 §38 | Versioned sport-adapter registry without product forks. | J12; packs | Implemented but disconnected | CAPS. Stage 4 conformance gate. |
| M39 | A1 §39 | Immutable versioned format packages bind defaults, validators, engines, tests and hints. | J12; packs/templates | Partial | Registries/platform templates exist; full package/migration binding incomplete. Stage 4 authority gate. |
| M40 | A1 §40 | Unsupported primitives fail explicitly; extensions are registered and deterministic. | J12; registries | Implemented but disconnected | CAPS/rulebook/dynamic/static fail-closed tests. Permanent gate. |
| M41 | A1 §41 | Persist compiler/runtime entities while keeping artefacts distinct. | J6/J11; platform | Partial | Many values exist, but compilation/simulation/proof records are not all authoritative persisted entities. Stage 1–3. |
| M42 | A1 §42 | Preserve Definition, Plan, Operational and Actual truth. | J8–J11; state | Partial | No-show revision 2 and court-outage revision 3 preserve immutable definition/plan artefacts, prior operational publications and completed/in-progress actual truth; other platform live paths remain separate. Stage 3. |
| M43 | A1 §43 | Competition truth is independent; integrations use explicit identity mapping. | J12; gateway | Implemented but disconnected | Signed integration gateway tested; production provider absent. Deferred to measured integration need. |
| M44 | A1 §44 | Command-oriented interpret→compile→validate→publish APIs. | J2–J6; journey/API | Partial | `/v1/competition-journey` exposes strict create/revise/compile/publish/read commands; legacy platform, pilot and web publication use the same identity/revision/acknowledgement-only contract and project only published truth through `/v1/tournaments`. Authenticated production composition is still required. Stage 2. |
| M45 | A1 §45 | Pure domain packages without UI/database/current-time/random leakage. | J12; architecture | Partial | UI separated, but one large engine package also owns persistence/integration. No split until stable journey seam. |
| M46 | A1 §46 | Same pinned inputs/versions/seed produce same outputs/proofs. | J11/J12; core | Implemented but disconnected | Canonical hash/replay/metamorphic tests. Permanent gate. |
| M47 | A1 §47 | AI interprets/explains/proposes only; deterministic code owns truth. | J3; AI boundary | Partial | The connected named-fixture journeys create, compile, Guard, publish, operate, repair, close, restore and duplicate with AI disabled. No evidence currently proves a configured or tuned on-device model, so AI remains an optional unverified advisory capability rather than product truth. Stage 1. |
| M48 | A1 §48 | Treat prompt/rulebooks as hostile; block code/authority/policy attacks. | J3/J12; security | Implemented and connected for named imports | `creation-source-import.test.ts` covers YAML executable/alias semantics, CSV formula and duplicate identity attacks, XLSX formulas/active content/archive safety/size boundaries, extra authority fields and quarantine. Generic roster tests additionally reject cardinality, division, pair-shape, partial/duplicate seed and cross-source identity/value conflicts plus a re-hashed derived-roster forgery on restart. |
| M49 | A1 §49 | Imported rulebooks retain citations, map to IR and require approval. | J2/J12; import | Partial | Cited candidate quarantine/review exists; PDF/site ingestion→pack absent. Deferred authority and corpus gate. |
| M50 | A1 §50 | Unit/integration/golden/property/regression/concurrency/replay/solver/compiler/mutation gates. | All; assurance | Broad 548-test TypeScript corpus plus 45 Swift tests; focused graph, source-derived roster and unavailable-solver regressions are executable; no general mutation runner/config found. Permanent gate; add campaign evidence. |
| M51 | A1 §51 | Solver cannot certify itself; shadow validation is independent. | J5/J6; Guard | Implemented and connected | Fresh journey uses scheduler shadow validation and a separate Guard pass; KCG003/KCG004 independently derive contest cardinality from spec. Deeper independent graph reconstruction remains under G01. |
| M52 | A1 §52 | Store honest solver identity/status/objective/bound/gap/hashes. | J5/J6; scheduler | Partial | Rich exact/CP-SAT proof; default scheduler audit omits some detail and is FEASIBLE. Stage 2. |
| M53 | A1 §53 | Dry-run dashboard shows integrity/path/schedule/risk evidence. | J6; Studio | Reference/demo only | `/lab` reference only. Stage 2. |
| M54 | A1 §54 | Per-entrant min/max matches/rest/overlap proof. | J4/J5; analytics | Implemented but disconnected | `analytics.ts`; full per-path rest proof incomplete. Stage 2. |
| M55 | A1 §55 | Navigable tournament truth debugger links every derivation. | J6/J11; evidence | Partial | IDs/hashes exist; full chain UI missing. Deferred gate: support disputes require it. |
| M56 | A1 §56 | Follow gated archaeology→schema→graph→schedule→simulation→UI sequence. | Programme | Partial | Core gates exist; connected vertical order is explicitly superseded by A4/PRD, capability gates remain. No deprecation. |
| M57 | A1 §57 | P&K is the permanent difficult static benchmark without generic-core brand branches. | J12; fixtures | Implemented and connected | The original 47-pair/98-contest benchmark remains green; St Albans executes 48 pairs/13 pools/108 contests, while Harbour and Northside exercise different counts and structures through the same canonical journey without fixture branches. Permanent gate. |
| M58 | A1 §58 | Modified Americano is the permanent dynamic benchmark. | J12; dynamic | Implemented but disconnected | Dynamic tests. Deferred entry gate: platform dynamic-stage binding. |
| M59 | A1 §59 | Evidence-derived Native/Composable/Extension-required/Unsupported catalogue. | J12; catalogue | Reference/demo only | Capability ledger derives evidence; displayed only in lab/demo. Stage 4. |
| M60 | A1 §60 | An unseen format reaches approved live creation without reinterpretation or lost rules. | Product acceptance | Partial | The St Albans source now completes explicit decisions→compile→Guard→publication→live no-show repair→operational publication without reinterpretation, but it is a known P&K-family format rather than an unseen format. Stage 2–3. |

## A2 safeguard requirements: S01–S21

| ID | Requirement | Owner / surface | Status | Evidence, gap, phase |
|---|---|---|---|---|
| S01 | Separate interpretation from deterministic spec validation. | Journey/schema | Implemented and connected | Creation proposal remains non-executable; a separate journey command invokes deterministic schema/scenario validation. |
| S02 | Exact semantic diff before commit. | Revisions/review | Implemented and connected | St Albans registered edits return a hash-bound, non-mutating semantic and operational impact preview before apply/recompile. |
| S03 | Critical policy requires approved provenance. | Type checker/Guard | Implemented and connected | St Albans cannot compile until qualification, scoring, tiebreak, normalisation, withdrawal, authority, event date and timezone decisions are explicit; compiled critical rules carry source/decision evidence. |
| S04 | Prove path/advancement/resource completeness before solve. | Type checker/Guard | Partial | Strong separate checks; creation ledger incomplete. Stage 1. |
| S05 | Independently rederive qualifier/bracket/match counts. | Type checker/Guard | Implemented and connected | Guard KCG003/KCG004 recomputes contest count from the canonical spec and blocks coordinated graph/schedule omissions; broader path reconstruction remains G01. |
| S06 | Shadow-validate every solver output. | Scheduler/Guard | Implemented and connected | Fresh journey schedules through the existing solver/shadow-validation path, then runs Guard; mutation regression remains green. |
| S07 | Consequential output carries structured proof. | Core/evidence | Partial | Widespread hashes/evidence, not one full schema. Stage 1–3. |
| S08 | Unsupported/unproved becomes unresolved/blocked. | All core | Implemented but disconnected | Strong fail-closed tests. Permanent gate. |
| S09 | Pin packs/compiler/defaults/solver for replay. | Schema/platform | Partial | Spec pins versions; web/Mac drafts do not. Stage 1. |
| S10 | Store random seed, algorithm, inputs and outputs. | Draw/random | Implemented but disconnected | Random/draw tests. Permanent gate. |
| S11 | No executable generated policy. | Interpreter/registry | Implemented but disconnected | No eval path; registries/quarantine. Permanent gate. |
| S12 | Invariant firewall rejects impossible runtime mutation. | Live/core | Partial | Connected live commands use optimistic concurrency, idempotency and the existing invariant firewall; approval and restart independently replay the event chain. Other platform writes still need migration. Stage 3. |
| S13 | Type-check shapes/cardinality/results/dependencies before solve. | Schema | Implemented but disconnected | Type checker and schema tests. Stage 1. |
| S14 | Universal proposal→validate→apply immutable revision flow. | Journey/platform | Partial | Source edits and no-show repair both preview without mutation, validate independently and apply only through separately approved immutable publication; remaining live incident types need migration. Stage 3. |
| S15 | Tournament Critic is read-only. | Critic/review | Implemented but disconnected | `compiler-critic.ts` tests. Stage 2. |
| S16 | Every source requirement has coverage/relaxation/unresolved/failed+counterexample. | Ledger/Guard | Implemented but disconnected | Critic/path proof; creator ledger missing. Stage 1. |
| S17 | Fuzz ugly cases and retain regressions. | Assurance | Implemented but disconnected | ASSURANCE/nasty fixtures. Permanent gate. |
| S18 | Show KNOWN/DERIVED/DEFAULTED/OPTIMISED/RANDOMISED/UNRESOLVED. | Review UX | Partial | Workbench API separates explicit rules, derived facts, organiser decisions, conflicts, untrusted claims and unresolved decisions; polished cross-format UI remains. Stage 2. |
| S19 | State factual validation, never blanket certainty. | Guard/UX | Implemented but disconnected | Certification/solver statuses; no blanket claim found. Stage 1–2. |
| S20 | Why-not identifies exact blockers and smallest/earliest correction. | Evidence/repair | Partial | The connected no-show preview returns affected scope, consequences and a proof that zero moved contests meets the lower bound; general scheduling correction UX remains. Stage 2. |
| S21 | Simple/detailed/technical views project the same rule. | Review UX | Implemented and connected for Guard pre-flight | One report hash and evidence hashes bind the short decision, expanded findings/accounting and technical proof view in the journey DTO and organiser page. |

## A3 Guard requirements: G01–G14

Ledger identity check: `G01`–`G14` are unique and `G09` appears exactly once.

| ID | Requirement | Owner / surface | Status | Evidence, gap, phase |
|---|---|---|---|---|
| G01 | Independently validate roster/pools/counts/qualification/cups/byes/ties/normalisation/rematches. | Guard/core | Implemented and connected for pilot envelope | KCG003/KCG004 rederive cardinality and KCG005 separately reconstructs St Albans pool pair coverage, opening draw/byes, round feeders and qualification dependency closure. Actual live scores independently rerun standings and the approved normalised qualification before identities resolve. General non-pilot loser paths and hard rematch policies remain Stage 4. |
| G02 | Validate schedule collisions, calendars, duration, rest, window and quality. | Guard/schedule | Partial | St Albans validates 108 translated assignments against identity, collisions, participant paths, changing calendars, variable durations, hard stop and registered final resources; the court-outage candidate is recomposed and revalidated across the same 108-contest schedule. Quality/resilience remains outside Guard. Stage 2. |
| G03 | Bottom-up contest/minute/capacity ledger; zero missing/duplicate. | Guard/evidence | Implemented and connected for pilot envelope | Guard 1.2 reconciles exact contest identities and contest/changeover/occupied minutes by division, stage, pool and round, plus availability/occupancy/spare capacity for every resource unit. Missing, duplicate, unexpected and duration-divergent contests fail reconciliation. |
| G04 | Validate every possible advancement path and correction invalidation. | Guard/live | Implemented and connected for pilot envelope | KCG005 independently reconstructs every St Albans winner feeder and qualification dependency; possible-entrant scheduling remains separate from server-derived actual entrants, unresolved starts and forged identities fail closed, actual winners advance and replay verifies mappings. Non-pilot loser-path correction invalidation remains Stage 4. |
| G05 | Govern live-change authority, preservation, impact, notification and recovery. | Live Guard | Partial | No-show, court-outage and delay paths prove actual-truth preservation, affected scope, independent authority, no pre-approval notification and exact targeted delivery through sequential operational revisions. Durable retry/idempotency/provider receipts are connected; external provider/fallback selection remains. Stage 3. |
| G06 | Short pre-flight with expandable evidence and correction. | Review UX | Implemented and connected for pilot envelope | The authoritative journey DTO and organiser pre-flight page project a short outcome, expandable section/accounting evidence, per-finding corrections and technical hashes from the exact Guard report. |
| G07 | Deterministic six-level severity/override policy. | Guard | Implemented and connected | The exported policy maps Critical/Integrity to block, Operational to exact acknowledgement, and Experience/Optimisation/Information to allowed warning; deterministic classifier tests exercise all six. |
| G08 | Same Guard for creation/import/manual/AI/repair/every client. | Journey/Guard | Partial | Mac Describe/Quick/exact JSON/YAML/CSV/XLSX import, web creation, legacy publication, no-show, court-outage and delay repairs converge before the same server-owned Guard with AI absent. Remaining incident formats still need migration. Stage 3. |
| G09 | Stable rule/entity/message/evidence/suggested correction. | Guard | Implemented and connected for pilot envelope | Every Guard finding carries stable source rule/path, sorted entities, message, machine evidence hash, publication disposition and deterministic suggested correction into the pre-flight projection. Broader entity schemas remain Stage 4. |
| G10 | Integrity grade is separate from operational quality. | Guard/quality | Implemented and connected for pre-flight | The same projection reports the certification integrity grade independently from allowed/acknowledged operational-quality findings; a quality warning cannot masquerade as mathematical certification or rejection. |
| G11 | Publication covers accounting/collisions/availability/dependencies/diff atomically. | Platform/Guard | Implemented and connected | Journey and platform publication load exact authoritative artefacts, independently rerun Guard, verify fresh approval hashes and atomically append publication/outbox. Guard 1.2 binds reconciled accounting, collisions, availability and dependencies; certificate 1.1 also binds the exact server-derived semantic/operational change set reviewed before recompilation. Closure and release verification fail closed on diff substitution. |
| G12 | Structure Guard covers pools/qualification/normalisation/cups/seeds/byes/play-ins/rematches. | Guard | Implemented and connected for pilot envelope | Guard 1.2 independently reconstructs the pilot's pools, qualification closure, cup rounds and byes; separate standings/qualification verification covers the approved percentage normalisation. Play-ins and loser/rematch policies remain preserved non-pilot Stage 4 evidence. |
| G13 | Live Guard covers correction/delay/withdrawal/no-show/repair/notification/invalidation. | Live/platform | Partial | Withdrawal/no-show, walkover repair, score correction lineage and targeted durable delivery are connected through replay/preservation checks and separate approval. Delay/court-outage repair and an external provider remain. Stage 3. |
| G14 | Every generated blueprint must pass Guard before use/publication. | Journey/Guard | Implemented and connected for pilot runtime | Connected journey and platform revisions cannot publish unless a fresh server-run Guard passes over the authoritative artefact set. Reference generators and local drafts are isolated from default runtime and production routes and have no publication path. |

### Critical Guard counterexample

The audit reproduced a coordinated omission against the baseline: removing one real P&K contest, its edges and schedule assignment, changing both graph counts to 97, clearing findings and omitting optional simulation previously returned `PASSED`. KCG003/KCG004 now independently rederive required cardinality. Guard 1.2 adds KCG005 path reconstruction plus a bottom-up accounting reconciliation, which blocks count-preserving coordinated rewires and makes missing, duplicate, unexpected or duration-divergent schedule facts visible in the same report. Every publication client sends only competition identity, expected revision and acknowledgements. The server loads the tenant-bound authoritative specification/graph/schedule/simulation, recomputes all hashes, reruns Guard, verifies a fresh exact-revision approval by a separate actor, and writes publication plus outbox atomically. `guardInput`, direct client artefacts and the retired standalone certification command fail closed at both API and in-process boundaries. Broader loser-path and hard-rematch reconstruction remains explicitly non-pilot Stage 4 work rather than a hidden pilot claim.

## A4–A8 later research and product requirements

| ID | Source | Requirement | Job / owner / surface | Status | Evidence, entry gate or decision |
|---|---|---|---|---|---|
| J01 | A4 Job 1 | Isolated organisation home, roles, directories, defaults and portfolio. | Platform; portfolio | Partial | PLATFORM backend/tests; demo UI and production composition disconnected. Stage 1; tenant deployment gate. |
| J02 | A4 Job 2 | Describe/Quick/Import/Duplicate converge on one resumable draft and requirement ledger. | Journey; creation | Implemented and connected for named envelopes | Web and Mac Describe/Quick/JSON/YAML/CSV/XLSX plus closure-bound duplicate converge under one sealed, versioned journey. The web draft can append or reversibly remove CSV/XLSX entrant evidence without starting another workspace. Originals/hashes, field provenance, quarantine/conflicts and restart replay are executable. Harbour and Northside now compile and duplicate their imported stable roster identities rather than placeholders; broader formats remain governed follow-ons. |
| J03 | A4 Job 3 | Explicit rules, assumptions, ambiguity, type checking and zero lost requirements. | Journey/Guard; review | Partial | St Albans exposes 192 entrant-field provenance facts, untrusted claims, conflicts and eight missing critical decisions. Generic connected blueprints retain field-level entrant and rule provenance, visibly require a roster and explicit registered scoring, tiebreak, withdrawal, draw and timezone semantics, and fail closed on roster/model disagreement. General fresh-format authoring and richer uncertainty UI remain. Stage 2. |
| J04 | A4 Job 4 | Fair deterministic structure, unequal pools, qualification, draw and path proof. | Core; review | Implemented and connected for named envelopes | St Albans preserves 13 exact pools and approved percentage-normalised Konnect/Tower qualification; the same canonical core produces a 15-contest six-pair round robin and a seven-contest eight-pair knockout, then resolves actual winner paths without trusting planning simulation identities. |
| J05 | A4 Job 5 | Physical resource schedule, lower bounds, quality and independent validation. | Scheduler/Guard; schedule | Partial | St Albans' 108-row source schedule is translated and independently checked; Harbour and Northside derive authoritative schedules from explicit resource calendars, duration and rest constraints and pass the same Guard accounting. Connected generic compilation now uses the pinned CP-SAT solver and independently validates its complete assignment; an unavailable backend fails before an approvable candidate exists. Rich quality comparison remains. Stage 2. |
| J06 | A4 Job 6 | Compare, prove, separately approve and atomically publish one exact revision. | Journey/platform; review | Partial | St Albans, Harbour and Northside retain semantic impact, independently Guard the exact compiled artifacts, require separate approval and atomically publish the revision/outbox. The connected web draft now drives that same server-owned compile/Guard/acknowledge/publish boundary and sends only expected draft/revision identity plus the exact acknowledgement set. Multiple schedule-option comparison remains. Stage 2. |
| J07 | A4 Job 7 | Install-free signed personal next action under one published revision. | Public projection | Implemented and connected | `/next` consumes an opaque signed expiring token and returns only identity, next fixture, court, reporting time, status, freshness and participant-impact revision from the authoritative publication/live head. Versioned-key rotation, participant revocation, audit replay, generic event QR and rate-limited private-code recovery are connected. Enumeration, forged/cross-organisation/expired/revoked tokens, stale heads and unpublished state fail closed. Stage 2. |
| J08 | A4 Job 8 | Action-first control room with exact idempotent/offline/stale commands. | Operator/live | Partial | Strict server-attributed commands use optimistic concurrency, content-bound idempotency and immutable replay. The Mac durably queues every essential command shape, calls the exact boundary, shows retry/conflict/reconciliation and cannot reorder past a conflict; richer per-command input UI remains. Stage 3. |
| J09 | A4 Job 9 | Typed disruption, scoped freeze, minimum-change repair, approval and targeted notify. | Live/repair | Implemented and connected | St Albans and the named six-pair round robin exercise no-show revision 2 through the same deterministic repair boundary; St Albans court-outage and delay/overrun revision 3 paths derive repairs from server-owned assignments, calendars and actual state. All preserve actual truth, rerun independent Guards, require independent approval and atomically target affected consumers. |
| J10 | A4 Job 10 | Separate safety authority, degraded/offline operation, emergency pack and restore. | Operations/platform/Mac | Partial | Signed offline truth, participant lookup, durable live/incident/stop journal, six-state safety authority, typed connected incident capture, authorised restart and exact-revision printable schedule/court/score/QR/restoration materials are connected. Authorised venue/emergency details and the staff-practised restore rehearsal remain. Stage 3. |
| J11 | A4 Job 11 | Close, replay, export, restore and duplicate with provenance. | Platform/evidence | Implemented and connected for named envelopes | St Albans, Harbour and Northside complete actual results, closure, evidence export, isolated restore/replay and clean duplication without copying live/result state. Managed production backup and staff rehearsal remain OPS07/deployment gates. |
| J12 | A4 Job 12 | Governed deterministic sport/format extension with honest envelopes. | Packs/assurance | Partial and connected | Registered padel pair round-robin and single-elimination semantics now enter the existing canonical compiler/Guard/journey without fixture code. Unsupported Swiss and every unconnected policy fail closed; broader sport packs, dynamic formats and the product catalogue remain governed Stage 4 work. |
| A5-J01..J15 | A5 journeys | Workspace creation/invite/switch, create/publish, reuse, participant, operations, repair, correction, sharing, domains, privacy, support and exit. | Platform/all surfaces | Partial | Platform covers many backend actions; public/domain/support/connected clients remain partial. Stages 1–4; no silent deprecation. |
| ISO01 | A5 isolation 1 | Immutable organisation ID; aliases never authority. | Platform/security | Partial | IDs/tenant tests; host resolver not production connected. Stage 1. |
| ISO02 | A5 isolation 2 | Resolve host/path to tenant before business query and match membership. | API/security | Missing | No custom-host tenant resolution runtime. Deferred entry gate: production public domain work. |
| ISO03 | A5 isolation 3 | Every row/stream/command/snapshot/outbox is tenant scoped; forced RLS. | Database | Partial | Postgres seams/migrations/tests; managed deployment/RLS drill outstanding. Production gate. |
| ISO04 | A5 isolation 4 | Repositories require organisation context; cross-references fail closed. | Platform | Implemented but disconnected | PLATFORM tenant tests; v1 reference API bypasses platform state. Stage 1. |
| ISO05 | A5 isolation 5 | Tenant-scope caches/files/CDN/exports/temp paths. | Platform/infra | Partial | Some exports/integration scope; full cache/object-store surface absent. Production gate. |
| ISO06 | A5 isolation 6 | Tenant-bind queues, webhooks, idempotency and signing keys. | Infra | Partial | Production boundary tested; provider deployment missing. Production gate. |
| ISO07 | A5 isolation 7 | Tenant-scope search/analytics/support views. | Platform | Partial | Analytics exists; full production search/support paths absent. Deferred measured need. |
| ISO08 | A5 isolation 8 | Tenant-bind secrets/payments; client cannot choose credentials. | Integrations | Partial | Secret-reference/security contracts; real integrations absent. Deferred integration gate. |
| ISO09 | A5 isolation 9 | Public pages use minimal publication-derived projections, never organiser aggregate. | Public web | Implemented and connected | `/competitions/:id`, `/next` and the venue display derive minimal separate projections from the publication and live head; the organiser aggregate is a different organisation-scoped endpoint. Stage 2. |
| ISO10 | A5 isolation 10 | Every happy path has cross-tenant host/token/cache/webhook adversarial pair. | Assurance | Partial | API/database/webhook tests; host/cache/file/public token coverage incomplete. Production gate. |
| A6-01 | A6 §1–5 | Organisation hierarchy and multi-sport dimensions compose around one truth. | Platform/packs | Partial | PLATFORM/CAPS disconnected. Stage 1/4. |
| A6-02 | A6 §6 | Sponsor/public aliases never mutate stable resources or proof. | Presentation | Adjacent/parked | Explicit A8 decision; entry gate: reliable public next-action plus sponsor need. |
| A6-03 | A6 §7 | Certified deterministic social graphics use certified public DTOs. | Presentation | Adjacent/parked | Research only; entry gate: accurate public delivery and rights/AX policy. |
| OPS01 | A7 §1 | Routine disruption and life safety are distinct authority systems with one history. | Operations | Implemented and connected | Competition repairs remain in the guarded live-revision path; life-safety modes and incidents use a distinct authority state inside the same sealed competition record. Safety stop outranks starts/publication. |
| OPS02 | A7 §2 | Named incident/referee/safety/comms/scribe functions and explicit command transfer. | Operations | Partial | Incident, competition, safety, communications and scribe assignments are explicit and transfer is evidence-bound; the non-production connected boundary owns actor attribution. Production membership/authentication composition and a polished command UI remain. Stage 3. |
| OPS03 | A7 §3 | Normal/Degraded/Paused/Stopped/Cancelled/Recovering state model. | Operations/public | Implemented and connected | All six modes, approved fixed messages, transition scope/time, OCC/idempotency, public/participant/Mac projection and replay proof execute on the St Albans journey. |
| OPS04 | A7 §4 | Detect→freeze→rank choices→validate→approve→publish→communicate→learn. | Journey/live | Partial | No-show connects the full routine repair loop. Typed incident detection and immediate safety freeze now share the history and public delivery, but external delivery and learning remain. Stage 3. |
| OPS05 | A7 §5 | Push/pull/ambient answers derive from authoritative truth with fallback. | Public/delivery | Partial | Participant pull, public venue display, organiser lookup and targeted message payload share the authoritative projection DTO and revision. External push provider and declared fallback remain blocked on operational selection. Stage 2–3. |
| OPS06 | A7 §6–7 | Governed weather/medical/venue readiness; software never diagnoses or invents thresholds. | Safety/pack | Missing | Deferred entry gate: venue/sport policy owners and pilot safety review. |
| OPS07 | A7 §8 | Signed offline truth, durable journal, reconciliation, outbox, restore and manual fallback. | Mac/platform | Partial | Signed scoped read truth includes the independently replayable safety proof and complete deterministic printable operational materials; the durable Mac journal captures live, incident and stop commands with explicit reconciliation, operational-head conflicts, server idempotency and restart-safe acknowledgement. The closed evidence bundle restores and independently replays in an isolated fresh store. Authorised emergency facts and the staff-practised disconnect/paper-to-restored-system rehearsal remain. Stage 3. |
| SURF01 | A8 | Install-free public web uses exact published minimal truth. | Public | Implemented and connected | The built Mac still opens the persisted exact published revision; desktop/mobile browser checks prove signed participant and public operational projections for St Albans and a named generic eight-pair competition. At 390 px the generic venue display showed imported participant names, published revision 1/operational revision 1 and no horizontal overflow. Production hosting remains a deployment concern. |
| SURF02 | A8 | Mobile/Mac/web are job-specific clients of identical IDs/revisions/permissions/API/events. | Clients/journey | Implemented and connected for named envelopes | Mac and web share journey identity, exact publication, true live head, proof-bound state, signed offline projections and strict durable commands. The web organiser lifecycle now compiles, independently Guards, acknowledges, publishes and activates the same authoritative journey, then opens exact revision-bound organiser/public projections. The default Mac and web portfolio contain only connected journey identities; seeded/reference surfaces are explicitly non-production and cannot serve pilot truth. |
| SURF03 | A8 | Mac is a professional offline client, not another truth/product. | Mac | Partial | Mac caches and verifies the signed server projection, links its bound printable operational pack and transports durable commands only: attribution, validation and truth remain server-authoritative, while stale work fails closed or becomes an explicit conflict. Venue-owned emergency authority and a practised restore remain Stage 3. |
| SURF04 | A8 | Adjacent commerce/ratings/booking/video/chat remain outside competition truth. | Product boundary | Adjacent/parked | Explicit decision; no implementation authorised in this programme. |
| AX01 | A8 / pilot gate | Core tasks support keyboard, assistive technology, zoom/reflow, contrast, reduced motion, print and outdoor/mobile use. | Accessibility/all surfaces | Partial | Participant, organiser, venue, closure and fallback pages have executable semantic/contrast/media contracts plus real-browser skip-navigation and 200–400% equivalent reflow evidence. Named VoiceOver/NVDA, printed-paper and outdoor-device inspection require the accessibility and pilot-operations owners before sign-off. |

## Delivered milestone and next decision

No authority conflict required product-owner direction. The smallest connected slice delivered here is:

1. `CompetitionJourney` orchestrates the existing parser, canonical schema, scenario/compiler and Guard components without a new solver or competition definition.
2. Language and quick-form input converge on one sealed, resumable `CompetitionBlueprint` draft; unknown fields and proposer artefacts fail closed.
3. The declared P&K envelope compiles locally and deterministically from server-owned source facts to 98 contests, schedule, simulation and Guard report.
4. `PUBLISH_TOURNAMENT` accepts only organisation/competition identity, expected revision and acknowledgements. The server loads and hashes the authoritative artefact set, independently reruns Guard, enforces compiler/approver/publisher separation and freshness, and atomically appends publication plus its exact-revision outbox intent.
5. The retired `guardInput`/standalone certification contracts and all client-provided specification, graph, schedule, simulation or Guard fields fail closed in legacy platform, pilot, web and direct in-process calls; adversarial, duplicate and restart/replay tests are executable.
6. The Mac presents review, required questions, assumptions/provenance, compilation, Guard acknowledgement and publication as separate stages, then opens the same immutable published web identity and revision.
7. The v1 portfolio/detail projection and responsive web page read only the persisted published journey revision; production v1 reads fail closed without authorization or an authoritative artefact resolver.
8. The same published St Albans revision activates the existing live event model. Check-in, start and completion facts are versioned and replayable; privileged identities and withdrawal/repair mutations are not accepted from connected clients.
9. A no-show produces two deterministic zero-move repair options with affected scope and consequences. Competition Guard and live Guard independently verify every option; a separate director approval atomically publishes operational revision 2 and only directly affected entrant outbox intents.
10. Signed `/next`, public venue and organiser projections derive from the exact published revision plus current operational/actual head; the mutable rehearsal singleton is removed.
11. Check-in, call, start, score, finish, approved walkover and score correction append immutable replayable facts, including after operational revision 2.
12. Targeted participant projection messages persist atomically with the live revision, retry idempotently and retain explicit provider evidence across restart.
13. Actual group results independently resolve the approved St Albans qualification into server-owned knockout identities; planned possible entrants cannot start or score as actual entrants, winner paths and `/next` update, and restart reproduces the same proof.
14. The Mac downloads an Ed25519-signed exact-revision event pack, verifies separately pinned trust, caches it per authoritative competition at 0600, supports 48-pair offline lookup and reloads it after restart. Forged, stale, cross-scope, expired and unpublished packs fail closed; missing emergency authority data is surfaced rather than fabricated.
15. A separate replayable operational-safety history now connects typed incidents, six explicit modes, named authority functions and transfer, fixed public instructions, immediate stop precedence and two-role restart clearance. The exact state/proof reaches the Mac pack, signed participant view, venue display and organiser projection; every public transition and 48 participant intents commit together.
16. The Mac can record typed incident facts offline and atomically add an immediate venue-stop intent to its scoped durable journal. Restart preserves both commands; reconnect uses distinct operational OCC, strict endpoints and server-owned actor/time/message/deadline, while stale state becomes a reviewable conflict instead of an overwrite.
17. The signed exact-revision envelope now includes deterministic printable order-of-play, per-court sheets, 108 manual score sheets, 48 opaque expiring participant QR cards and seven restoration steps. The client cannot inject material or query fields; missing coverage, stale revisions, mismatched hashes and forged signatures fail closed. The view visibly blocks emergency readiness until safety-owner facts exist.
18. The same St Albans identity now closes only after all 108 server-owned actual results are terminal, protests and appeals are resolved, operational authority is normal, Guard is independently rerun, four exact acknowledgements are present and the closer is separate. Closure, final proof and its outbox intent commit atomically; retry and restart replay exactly.
19. The closed competition exports a hash-bound machine/human evidence bundle, rejects forged nested artifacts, restores into an isolated fresh repository with independent replay and byte-identical re-export, and duplicates into a clean new edition carrying only explicit source/decision memory plus closure provenance. The duplicate recompiles to 108 contests without inheriting approval, publication, live or result state.
20. Connected participant, organiser, venue, closure and fallback surfaces now share an executable accessibility envelope and pass keyboard plus 200%/400% equivalent browser reflow on the exact St Albans state. That browser gate also corrected post-repair participant/QR URLs to operational revision 2 and repaired venue-status contrast.
21. JSON/YAML competition sources and CSV/XLSX entrant evidence now enter the same authoritative workbench. Exact originals/hashes, 192 entrant-field provenance facts, quarantine/conflicts, reversible last-source removal and restart replay are executable; the web creator no longer hands new drafts to the mutable platform demo.
22. Participant access now has an append-only, content-bound rotation/revocation/recovery history. A retained verification key permits explicit migration to the active key, revocation survives restart, generic event recovery discloses no participant identity and its private-code exchange is exact-head-bound and rate limited.
23. The connected Mac imports exact JSON/YAML/CSV/XLSX sources and appends them to the same server-owned journey without local competition interpretation. It rejects unsafe local file envelopes, and clean duplication first resolves the closed authoritative head before submitting only closure identity plus new-edition facts.
24. Connected `/v1/tournaments` portfolio and detail routes now expose only persisted authoritative journeys. An empty repository cannot produce a reference event, and unknown or reference-demo identities return 404 across blueprint, schedule, operations, findings and certification reads.
25. A trained organiser can duplicate the closed authoritative edition from the web view as well as Mac/API. The form sends only the current closure hash and new-edition name/date, announces success/failure accessibly and links to the resulting clean draft.
26. Guard 1.2 independently reconstructs all pool pair coverage, opening entrants/byes, winner-round feeders and qualification dependencies used by St Albans. Count-preserving feeder, opening-draw and qualification-edge mutations now fail as KCG005 before approval or publication.
27. The exact closed St Albans rehearsal now produces an executable, hash-bound release manifest. Software defects and missing external authority are distinct states; eleven provider, safety, rehearsal, accessibility, production, support and role-approval gates each expose their required owner and fixed safe fallback. No missing external fact is represented as passed.
28. Guard 1.2 adds the reconciled contest/minute/resource ledger and one connected organiser pre-flight. Simple, detailed and technical inspection levels share the exact report identity; every finding exposes stable evidence and a deterministic correction, while integrity and operational quality remain separate.
29. Publication certificate 1.1 binds a deterministic server-derived semantic and operational change set to the same exact revision as the Guard report. The change-set hash survives approval, atomic publication/outbox, restart, closure export and release assessment; independently rehashed tampering is blocked as a software defect.
30. The remaining mutable demo workspace is no longer a default product surface. `/` renders only durable journey records, all web reference/compiler routes are non-production, production fails closed without a real web projection, and the Mac launches with exactly one connected workspace. Preserved seeded profiles remain test/reference capabilities only.
31. Product flexibility is now executable rather than inferred from St Albans: Harbour Six-Pair Round Robin compiles to 15 contests and Northside Eight-Pair Knockout compiles to seven contests. Both exercise an approved no-show operational revision, signed participant/public/organiser projections, targeted outbox delivery, source-hash provenance, structured impact review, server-owned compilation, independent Guard, separate atomic publication, restart-safe live operation, winner resolution, closure, isolated restore and clean-edition duplication through `CompetitionJourney`; Swiss remains an explicit fail-closed unsupported semantic.
32. Generic entrants are authoritative product truth rather than generated display placeholders. Harbour and Northside now require accepted CSV/XLSX rosters, retain stable entrant/member identities and field provenance, independently reconstruct those rosters from preserved source bytes at compilation/restart, and carry the imported names through live operation, signed participant/public projections, repair, closure, restore and duplication. The connected web draft appends and reversibly removes those sources; count, identity, division, pair shape, seed, cross-source and re-hashed derived-roster attacks fail closed.
33. The connected organiser web page now drives a named source-ready competition through server-owned compilation, independent Guard, exact acknowledgements, separate atomic publication and live activation without submitting compiler or Guard artefacts. The generic eight-pair round robin uses the existing pinned CP-SAT boundary and publishes all 28 of 28 contests; an unavailable solver fails before a candidate exists. Its exact revision-bound organiser and venue projections show the imported participant identities at 390 px without horizontal overflow.
34. Source intake is now a staged organiser interaction rather than an XLSX-only native control or raw-text default: JSON, YAML, CSV and XLSX all expose Browse and drag/drop entry, show selected filename/size and an explicit removal path, and retain text paste only as a disclosed fallback. A primary source plus up to seven corroborating files is previewed, compared and atomically persisted as one authoritative draft; originals, hashes, provenance and conflicts remain visible rather than silently merged. The same strict server source boundary and quarantine chain remain authoritative; `creation-source-import.test.ts` proves bundle creation and browser evidence verifies staged CSV intake at 390 px without horizontal overflow.
35. The default authoritative competition route is now **Organiser Studio**, not the legacy journey detail. It makes the J1/J2 Competition Design, Run Assurance and Publish Review modes legible while retaining the same version-bound source, compile, Guard, acknowledgement, publication and activation commands. Source corroboration remains inside Competition Design and preserves its optimistic draft version. The allowed J5 **Close & Integrity Receipt** is a separate closed-evidence projection: it cannot calculate or certify anything, derives closure from the exact journey head, submits only closure revisions and four fixed acknowledgements, exposes the bound evidence bundle and duplicates only from the closure hash. `authoritative-surface.test.ts` and `creation-source-import.test.ts` exercise the rendered routes and the full TypeScript/test gate proves the existing authoritative boundaries remain intact.
36. **Run Control** now exposes the existing J3 live-operation boundary as an action-first operator surface rather than a read-only participant list: check-in, call, start, score, finish and walkover build only one strict, expected-operational-revision command at a time. The server continues to attribute actor/time, apply idempotency and optimistic concurrency, preserve immutable Definition/Plan/Operational/Actual layers, append facts and atomically generate affected delivery evidence. Guarded disruption remains a separately approved Change Review path; it is not disguised as an ordinary live command. `studio.test.ts`, `pilot-accessibility.test.ts` and the complete journey/adversarial/replay suite cover the renderer contract and unchanged server enforcement.
37. **Change Review** is now contextual within Run Control (J4, Live Operation), not a standalone assurance surface. It sends only the current operational/live revisions and no-show identity/reason to the existing server preview boundary; displayed choices are server-derived and independently Guarded, and approval sends only the proposal hash plus one registered strategy to the separately attributed director boundary. Stale, forged and pre-approval paths remain rejected by the existing journey tests. The composition contract is covered by `studio.test.ts`; the serial full suite is used for this gate because the unrelated dynamic-stage SQLite test has a known parallel lock race.
38. The release authority is now an explicit blocker package rather than a generic “external work” label. Each genuine external gate records the accountable role, exact decision required, required scope-bound evidence and safe fallback in `docs/pilot-release-readiness.md`. The executable `assessPilotRelease` gate still refuses every missing or malformed acceptance; this documentation adds no invented credential, safety fact or human approval.
39. Run Control Change Review now composes all three connected J4 repair types—no-show, court outage and delay/overrun—inside one Live Operation surface. Each preview sends only the exact current head plus incident facts; the server derives candidates, reruns the appropriate independent Guard and returns the proposal hash. Approval remains a distinct revision-bound command, and the resulting operational revision targets only affected projections/outbox intents. `studio.test.ts` asserts the surface contract while the St Albans no-show, court-outage and delay journey tests prove the protected boundaries, replay and adversarial cases.
40. The accessibility gate now evaluates the routed `runControlHtml`, including its contextual Change Review, rather than only the base organiser projection from which it is composed. The executable surface test covers its landmark, skip link, reduced-motion, contrast, forced-colour, print, status-region and named Change Review relationships; the human assistive-technology/outdoor acceptance remains an explicit external gate.
41. Large narrative intake remains bounded for review safety but is no longer constrained by the obsolete 12,000/48,000-character creator limit: the same authoritative language proposal endpoint and parser accept up to 256,000 characters, enough for the full St Albans operational narrative. Larger material must be split into preserved source documents rather than silently truncated or treated as executable instruction. `creation-proposal.test.ts` proves acceptance within the bound and fail-closed rejection beyond it.
42. **Run Control incident usability (J3/J4 · Live Operation · server-owned operational head · Run Control):** the operator surface now reads the existing runtime control-room queues as prioritised Now, Next, Late, Blocked and Needs Attention rows before any form. Its staff-only `controlContests` projection carries only runtime-owned resolved fixture-side IDs, court and status; score and walkover commands use those IDs and never infer entrants from display names. Change Review shows only no-show, court-outage or delay inputs relevant to the selected type, then renders the server-derived consequence order (incident, affected scope, changes, operational/fairness impact, independent Guard proof) before explicit Reject, Modify or hash-bound Approve. The browser/client remains non-optimistic: every success refreshes server evidence and every failure states that nothing changed. `live-no-show-journey.test.ts`, the existing St Albans court-outage/delay adversarial suites, `studio.test.ts` and `pilot-accessibility.test.ts` provide executable evidence; production identity, persistence, delivery-provider and human field-acceptance gates remain blocked in `docs/pilot-release-readiness.md`.

43. **Run Control live-incident browser rehearsal (J3/J4 · Live Operation · server-owned revision/Guard boundary · Run Control and signed participant web):** deterministic St Albans local-browser rehearsals complete no-show, court closure and delay/overrun through server-derived consequence review, independent competition/live Guard proof and separately attributed approval. The resulting operational revisions refresh the organiser queues; the affected signed `/next` projection renders only the participant's fixture, court, reporting time, status and exact revision. A court repair following a no-show retains the earlier walkover outcome in the current public/organiser projection; `live-court-outage-journey.test.ts` regresses that cross-repair invariant. Forged/stale/duplicate and client-artifact inputs remain covered by the corresponding no-show, court-outage and delay adversarial/replay suites. This is local evidence only and does not satisfy the unresolved production identity, durable-persistence, delivery-provider or human acceptance gates in `docs/pilot-release-readiness.md`.

44. **Run Control selected repair evidence (J4 · Live Operation · server-owned live revision/Guard boundary · Run Control):** the deterministic St Albans browser rehearsal at `2026-09-16` opened the server-derived no-show preview at operational revision 1 and visibly rendered both independently Guarded choices before approval. Each choice showed the same derived affected entrants/fixtures, its own consequences, zero-move lower-bound/slot-release impact, Competition Guard and live-Guard `PASSED` hashes, and its distinct option hash; Approve was disabled until a radio option was selected. The browser also opened a `LATE` attention row into the prefilled incident review with authoritative fixture context and `Attention: 60 minutes late`. `live-no-show-journey.test.ts` now proves a forged selected option hash is rejected without publication; `live-court-outage-journey.test.ts` and `live-delay-overrun-journey.test.ts` remain the executable deterministic/browser-rehearsal evidence for the other two existing incident paths.

45. **Organiser Studio structure revision (J2 · Competition Design → Certified Plan · server-owned `CompetitionJourney` draft/compile/Guard boundary · Organiser Studio):** the existing canonical definition projects a stable-ID Structure Map of pools, qualification edges and cups/brackets. Deterministic warning fixtures prove `INVALID_EDGE`, `INCOMPLETE_EDGE` and `CONSEQUENTIAL_EDGE` remain attached to the affected stable edge and are rendered there; a complete zero-edge round-robin/knockout structure is labelled as such rather than called incomplete. The browser submits only the one qualification-rule control exposed by this UI slice, its expected draft revision and the server-derived preview hash; this does not restrict the existing generic server structured-edit API. It never sends a graph, compiled plan, Assurance result or Guard artefact. Review renders server-derived match and qualification counts as before → after with an explicit delta for both production-lock and connected generic definitions, honestly labelling the delta unavailable only when either canonical definition is incomplete, while Assurance/Guard remain pending until exact compilation. `st-albans-workbench.test.ts` covers available and unavailable deltas plus forged, stale, replay, post-apply and publication-immutability paths; `flexible-competition-journey.test.ts` covers honest generic zero deltas; `structure-map-evidence.test.ts` covers warning attachment, rendering and complete zero-edge wording; `rehearse:organiser-structure-map-browser` records rendered stable IDs, warning codes, review values, preview hash and resulting revision in `output/playwright/organiser-structure-map-browser.json`. The independent goal gate deletes any prior declared artifact before rehearsal, then retains the regenerated artifact and its hash. Production deployment, Scenario Lab and unrelated Studio capabilities remain outside this slice.

Broader format/sport connection, non-pilot loser-path/hard-rematch reconstruction and a verified on-device advisory model remain governed follow-ons rather than St Albans deployment blockers. The remaining pilot gates require external authority or deployment inputs: provider/fallback selection and credentials, authorised emergency details, named-assistive-technology/outdoor accessibility acceptance, full staff/manual recovery rehearsal, production persistence/restore evidence, support assignment and final role approvals. The emergency pack cannot be declared ready until the venue/pilot safety owner supplies and approves the named responders, contacts, venue/access and evacuation facts; until then the explicit fallback is the venue's separately controlled printed safety plan. External delivery remains fail closed until the pilot owner selects credentials, a provider and its declared fallback. The executable release manifest lists every remaining gate, accountable role and fixed fallback against the exact closed rehearsal scope; no missing external fact is represented as passed.
